{
  "name": "mrpop",
  "version": "1.0.0",
  "description": "MrPop.io Monorepo Root",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend-landing\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend-landing",
    "build": "npm run build --prefix frontend-landing"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}